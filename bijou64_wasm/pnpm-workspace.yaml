allowBuilds:
  # esbuild ships a prebuilt platform binary via its `@esbuild/*` optional
  # deps, so its install script is unnecessary here. Mark it reviewed and
  # skipped to avoid the ERR_PNPM_IGNORED_BUILDS failure under pnpm v11.
  esbuild: false
