[workspace]
resolver = "3"
members = [
  "bijou64",
]

[workspace.package]
edition = "2024"
rust-version = "1.90"
license = "MIT OR Apache-2.0"
repository = "https://github.com/inkandswitch/bijou"
authors = [
  "Brooklyn Zelenka <hello@brooklynzelenka.com>"
]

[workspace.dependencies]
# Internal crates
bijou64 = { version = "0.1.1", path = "bijou64", default-features = false }

# External crates
arbitrary = "1.4"
bolero = "0.13.4"
criterion = { version = "0.5", default-features = false }
criterion_pprof = { package = "pprof", version = "0.15", default-features = false, features = ["criterion", "flamegraph"] }
rand = { version = "0.8.5", default-features = false }
testresult = "0.4.1"
thiserror = { version = "2.0", default-features = false }

[workspace.lints.rust]
future_incompatible = { level = "warn", priority = -1 }
let_underscore = { level = "warn", priority = -1 }
missing_copy_implementations = "warn"
missing_debug_implementations = "warn"
missing_docs = "warn"
nonstandard_style = { level = "warn", priority = -1 }
rust_2021_compatibility = { level = "warn", priority = -1 }

rust_2018_idioms = { level = "deny", priority = -1 }
unreachable_pub = "deny"
unused_extern_crates = "deny"

unsafe_code = "forbid"

[workspace.lints.clippy]
dbg_macro = "warn"
expect_used = "warn"
fallible_impl_from = "warn"
fn_params_excessive_bools = "warn"
indexing_slicing = "warn"
missing_const_for_fn = "warn"
must_use_candidate = "warn"
panic = "warn"
todo = "warn"
unneeded_field_pattern = "warn"
unwrap_used = "warn"
wildcard_enum_match_arm = "warn"

all = { level = "deny", priority = -1 }
cargo = { level = "deny", priority = -1 }
multiple_crate_versions = "allow" # various transitive deps
pedantic = { level = "deny", priority = -1 }

# Release profile is tuned for Wasm bundle size. Override per-package below
# to opt-level = 3 for crates that are not shared with the Wasm target.
[profile.release]
codegen-units = 1
lto = "fat"
opt-level = "z"
panic = "abort"
strip = "symbols"

# Benchmarks: keep symbols and debug info for flamegraphs/profiling tools.
# Note: the bench profile inherits from release, so we override opt-level back
# to 3 for accurate native benchmark numbers.
[profile.bench]
codegen-units = 1
debug = true        # Full debug symbols for flamegraphs/profiling
lto = "thin"        # Faster link than "fat", close enough for profiling
opt-level = 3       # Speed, not size
strip = false       # Keep symbols for profiling tools
